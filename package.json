{
  "name": "playwright-postgres-crud",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:db": "playwright test --workers=1",
    "report:clean": "rm -rf allure-results allure-report",
    "report:gen": "allure generate allure-results --clean -o allure-report",
    "report:open": "allure open allure-report",
    "test:allure": "npm run report:clean && npm run test:db && npm run report:gen && npm run report:open"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.2.3",
    "allure-commandline": "^2.36.0",
    "allure-playwright": "^3.4.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "private": true,
  "dependencies": {
    "dotenv": "^17.2.4",
    "pg": "^8.18.0"
  }
}
